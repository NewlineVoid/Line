{
  "randomNumber": {
    "name": "randomNumber",
    "aliases": ["random", "number"],
    "contributors": [
      {
        "name": "NewlineVOID",
        "role": "Creator",
        "socials": {
          "twitter": "newlinevoid",
          "discord": "697729838517190686",
          "github": "NewlineVoid"
        }
      }
    ],
    "description": "Retrieve random number",
    "examples": ["test 1 5 true"],
    "args": [
      { "name": "min", "type": "essential", "valueType": "int" },
      { "name": "max", "type": "essential", "valueType": "int" },
      { "name": "floor", "type": "optional", "valueType": "int" }
    ],
    "callback": "function(min, max, floor) { let number = Math.random() * (max-min)+min; if (floor) number = Math.floor(number); console.log(number) }",
    "permissions": ["admin", "user"]
  }
}

